       -h     Print help message.

       -l     List all previews.

       -m file ...
              Print extension and MIME type of file.

       -c     Clear currently displayed preview image (for kitty terminals).

       -e     End preview session and cleanup (for kitty terminals).

       -d     Enable debug messages.
